require 'swagger_helper'

RSpec.describe 'api/v1/botols', type: :request do

  path '/api/v1/botol/datatable' do

    post('datatable botol') do
      response(200, 'successful') do

        after do |example|
          example.metadata[:response][:content] = {
            'application/json' => {
              example: JSON.parse(response.body, symbolize_names: true)
            }
          }
        end
        run_test!
      end
    end
  end

  path '/api/v1/botol/{uuid}' do
    # You'll want to customize the parameter types...
    parameter name: 'uuid', in: :path, type: :string, description: 'uuid'

    delete('delete botol') do
      response(200, 'successful') do
        let(:uuid) { '123' }

        after do |example|
          example.metadata[:response][:content] = {
            'application/json' => {
              example: JSON.parse(response.body, symbolize_names: true)
            }
          }
        end
        run_test!
      end
    end

    patch('update botol') do
      response(200, 'successful') do
        let(:uuid) { '123' }

        after do |example|
          example.metadata[:response][:content] = {
            'application/json' => {
              example: JSON.parse(response.body, symbolize_names: true)
            }
          }
        end
        run_test!
      end
    end

    get('show botol') do
      response(200, 'successful') do
        let(:uuid) { '123' }

        after do |example|
          example.metadata[:response][:content] = {
            'application/json' => {
              example: JSON.parse(response.body, symbolize_names: true)
            }
          }
        end
        run_test!
      end
    end
  end

  path '/api/v1/botol/{uuid}/harga' do
    # You'll want to customize the parameter types...
    parameter name: 'uuid', in: :path, type: :string, description: 'uuid'

    get('show_harga botol') do
      response(200, 'successful') do
        let(:uuid) { '123' }

        after do |example|
          example.metadata[:response][:content] = {
            'application/json' => {
              example: JSON.parse(response.body, symbolize_names: true)
            }
          }
        end
        run_test!
      end
    end

    post('create_harga botol') do
      response(200, 'successful') do
        let(:uuid) { '123' }

        after do |example|
          example.metadata[:response][:content] = {
            'application/json' => {
              example: JSON.parse(response.body, symbolize_names: true)
            }
          }
        end
        run_test!
      end
    end

    patch('update_harga botol') do
      response(200, 'successful') do
        let(:uuid) { '123' }

        after do |example|
          example.metadata[:response][:content] = {
            'application/json' => {
              example: JSON.parse(response.body, symbolize_names: true)
            }
          }
        end
        run_test!
      end
    end
  end

  path '/api/v1/botol/list' do

    get('botol_list botol') do
      response(200, 'successful') do

        after do |example|
          example.metadata[:response][:content] = {
            'application/json' => {
              example: JSON.parse(response.body, symbolize_names: true)
            }
          }
        end
        run_test!
      end
    end
  end

  path '/api/v1/botol' do

    post('create botol') do
      response(200, 'successful') do

        after do |example|
          example.metadata[:response][:content] = {
            'application/json' => {
              example: JSON.parse(response.body, symbolize_names: true)
            }
          }
        end
        run_test!
      end
    end
  end
end
